{
  "name": "our-website",
  "version": "1.0.0",
  "main": "public/script.js",
  "scripts": {
    "start": "live-server --port=3000 --no-browser --entry-file=index.html --mount=/public:./public --mount=/protected:./protected",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
